fastapi
uvicorn
python-dotenv
psycopg2-binary
# SQLAlchemy queda por debajo de 2.1 A PROPÓSITO. En 2.1 el DBAPI por defecto de
# `postgresql://` pasó de psycopg2 a psycopg (v3), que no está instalado, así que
# el primer build que la agarró tiró la producción abajo con
# `ModuleNotFoundError: No module named 'psycopg'` antes de correr las
# migraciones. El pin es la mitad barata del arreglo; la otra —mudar el driver a
# psycopg 3— es un cambio de verdad y se hace con la suite delante, no con el
# backend caído.
sqlalchemy<2.1
alembic
pyjwt[crypto]
httpx
resend
# zoneinfo necesita una base IANA completa: la imagen de Railway trae una tzdata
# del sistema SIN los links de compatibilidad ("America/Buenos_Aires"), que es
# justo lo que mandan los navegadores. Este paquete es el respaldo de zoneinfo.
tzdata
# Minijuego de derivadas (backend/game/): deriva con sympy.diff y valida por
# equivalencia numérica con lambdify. Sin numpy ni antlr4 a propósito.
sympy
# El oyente de donaciones de Cafecito (backend/game/cafecito_stream.py) habla
# socket.io a mano sobre un WebSocket: es lo que hace que el empuje aparezca en el
# momento en que alguien dona, en vez de tener que aplicarlo a mano.
websocket-client
