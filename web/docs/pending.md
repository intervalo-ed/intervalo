- Check `sessions` table columns work as expected. 
-- To calculate duration session: if after 30min no question is answered, the session is considered abandoned.

- Check `answers` table columns work as expected.

- Check `unit_states` table columns work as expected.