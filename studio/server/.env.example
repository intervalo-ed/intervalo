# Copiá este archivo a .env y completá tu API key de OpenRouter.
# El .env real está git-ignored: nunca se commitea ni llega al frontend.
OPENROUTER_API_KEY=sk-or-...

# Modelo por defecto (opcional). Cualquier id válido de OpenRouter.
OPENROUTER_MODEL=anthropic/claude-sonnet-4.5
